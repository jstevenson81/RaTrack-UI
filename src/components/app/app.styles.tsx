import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    topDiv: {
      marginTop: 25,
      paddingRight: 15,
      paddingLeft: 15
    }
  });

export default styles;